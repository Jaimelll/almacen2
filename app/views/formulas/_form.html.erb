<%= form_for([@product,@formula]) do |f| %>
  <% if @formula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@formula.errors.count, "error") %> prohibited this formula from being saved:</h2>

      <ul>
      <% @formula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :material %>
    <%= f.collection_select :material, Product.all.order(:nombre), :id, :nombre, {:include_blank => ' falta material'} %>

  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :cantidad %>
    <%= f.text_field :cantidad %>
  </div>

  <div class="field">
    <%= f.label :orden %>
    <%= f.text_field :orden %>
  </div>

<br/>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
